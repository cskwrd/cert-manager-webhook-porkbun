apiVersion: apiregistration.k8s.io/v1
kind: APIService
metadata:
  name: v1alpha1.{{ .Values.groupName }}
  labels:
    app: {{ include "porkbun-webhook-solver.name" . }}
    chart: {{ include "porkbun-webhook-solver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    cert-manager.io/inject-ca-from: "{{ .Release.Namespace }}/{{ include "porkbun-webhook-solver.servingCertificate" . }}"
spec:
  group: {{ .Values.groupName }}
  groupPriorityMinimum: 1000
  versionPriority: 15
  service:
    name: {{ include "porkbun-webhook-solver.fullname" . }}
    namespace: {{ .Release.Namespace }}
  version: v1alpha1
